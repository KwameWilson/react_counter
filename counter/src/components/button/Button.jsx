import React from "react";
import './button.css'

export const Button = (props) => {
    return (

    const [counter] 

        <div>
            <button>{props.function}</button>
        </div>
    )
}